<h1>
  <%= @room.title %>
</h1>

<ul>
  <p>メンバー</p>
  <% @room.groups.each do |room_user| %>
  <li><%= room_user.user.name %></li>
  <% end %>
</ul>
<%= link_to '削除', @room, method: :delete, data: { confirm: "タスク「#{@room.title}」を削除してもよろしいですか？"}, class: 'btn btn-danger' %>